{
  "name": "supply-chain-blockchain",
  "version": "0.1.0",
  "private": true,
  "description": "Hardhat: SupplyChainRegistry compile, test, deploy",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:local": "hardhat run scripts/deploy.js --network localhost",
    "node": "hardhat node"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "hardhat": "^2.22.17"
  }
}
