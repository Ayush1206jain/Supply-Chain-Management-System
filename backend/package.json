{
  "name": "supply-chain-backend",
  "version": "0.1.0",
  "private": true,
  "description": "REST API for blockchain-backed supply chain (Express, JWT, DB)",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "mongoose": "^8.9.3"
  }
}
